[project]
name = "zulip-rulesbot"
version = "0.1.0"
description = "Python hello world worker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "zulip==0.9.1",
    "zulip-bots==0.9.1"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
